import { Box, Typography, Card, CardContent } from '@mui/material';

const PostFeed = () => {
  // This will be implemented when you add post functionality
  return (
    <Box>
      <Typography variant="h5" gutterBottom>
        Post Feed
      </Typography>
      <Card>
        <CardContent>
          <Typography color="text.secondary">
            Post feed functionality will be implemented in the next phase. This is where posts from
            all users will be displayed.
          </Typography>
        </CardContent>
      </Card>
    </Box>
  );
};

export default PostFeed;
